Security notes:
- Never commit private keys into Git repositories.
- Use secrets management for passwords (Vault/GitLab CI variables).
- Protect the CA private key with hardware security module (HSM) in production.
