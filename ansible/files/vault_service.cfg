define command{
  command_name    check_vault
  command_line    $USER1$/check_vault.sh
}

define service{
  use                     generic-service
  host_name               vault-server
  service_description     Vault Health
  check_command           check_vault
  check_interval          1
  retry_interval          1
}
